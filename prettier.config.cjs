module.exports = {
  semi: true, // Add semicolons at the end of statements
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'all', // Add trailing commas wherever possible (in multi-line objects, arrays, etc.)
  printWidth: 80, // The maximum line length before wrapping
  tabWidth: 2, // The number of spaces per indentation level
  useTabs: false, // Indent with spaces instead of tabs
  arrowParens: 'always', // Always include parentheses around arrow function arguments
  jsxBracketSameLine: false, // Place the closing bracket of JSX elements on a new line
};
